let maskEmail = document.getElementById("maskEmail")
let email = document.getElementById("email");

function myEmail() {
    let emailIndex = email.indexof("@");
    let username = email.slice(0, emailIndex);
    let end = email.slice(emailIndex);
}
